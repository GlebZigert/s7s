package dispatcher
/*
import (
    "os"
    //"log"
    "encoding/json"
)

func (dispatcher *Dispatcher) loadConfig(filename string) (error) {
	/*file, err := os.Open(filename)
	if err != nil {
		return err
	}
	defer file.Close()
	decoder := json.NewDecoder(file)
	err = decoder.Decode(c)
	return err
}


func (dispatcher *Dispatcher) saveConfig(filename string) (error) {
    file, _ := os.OpenFile(filename, os.O_CREATE, 0644)
    defer file.Close()  
    encoder := json.NewEncoder(file) 
    encoder.Encode()
}*/